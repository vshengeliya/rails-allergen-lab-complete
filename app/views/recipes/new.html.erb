<h1>New Recipe</h1>

<%= form_for @recipe do |f| %>
  Name: <%= f.text_field :name %><br />
  Cousine : <%= f.text_field :cuisine %><br />
  What User?:<%=f.collection_select( :user_id, User.all, :id, :name)%>
  <%= f.submit "Create a new recipe" %>
<% end %>

<%= link_to 'Back', recipes_path %>
